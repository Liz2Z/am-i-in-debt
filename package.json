{
  "name": "coding-plan-usage-tray",
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "get-cookies-script"
  ],
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:sidecar": "cd get-cookies-script && bun run build",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "vite": "^6.0.0"
  }
}
